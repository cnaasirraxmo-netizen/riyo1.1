# RIYOBOX Web Admin Panel

A professional web dashboard for managing the RIYOBOX streaming application.

## 🚀 Deployment to Vercel

1.  **Move to the web_admin directory:**
    ```bash
    cd web_admin
    ```
2.  **Install dependencies:**
    ```bash
    npm install
    ```
3.  **Build the project:**
    ```bash
    npm run build
    ```
4.  **Deploy:**
    Upload this directory to Vercel or connect your GitHub repository. Vercel will automatically detect the settings from `vercel.json`.

## 🛠 Features

*   **Secure Dashboard:** Overview of system health and stats.
*   **Content Management:** Upload, view, and delete movies in real-time.
*   **User Management:** Monitor active users and roles.
*   **Modern UI:** Built with React, Tailwind CSS, and Lucide Icons.

## 🛡 Login

Use your admin credentials:
*   **Email:** `admin@example.com`
*   **Password:** `admin123`
